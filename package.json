{
  "name": "sheet-messenger",
  "version": "0.3.0",
  "private": true,
  "description": "Minimal messenger powered by Google Apps Script and Google Sheets",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node --check app.js && node --check config.js && node --check sw.js && node --check scripts/dev-server.mjs && node --check scripts/test-backend.mjs && node -e \"new Function(require('fs').readFileSync('apps-script/Code.gs', 'utf8'))\"",
    "test": "node scripts/test-backend.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
